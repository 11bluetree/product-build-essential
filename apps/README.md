# apps

このディレクトリは、アプリケーションコードを配置する場所です。

複数アプリで再利用するコードは `packages/` に配置し、ここにはアプリ固有のコードを置きます。

モノレポ構成の場合は、ここにアプリ単位のディレクトリを作成します。例:

- `frontend/` と `backend/`
- `client/` と `api/`

命名はプロジェクトの方針に合わせつつ、責務が分かる名前にしてください。
